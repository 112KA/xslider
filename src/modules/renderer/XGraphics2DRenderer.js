import {BaseRenderer} from './BaseRenderer'

export class XGraphics2DRenderer extends BaseRenderer {
	constructor() {
		super();
	}
	
	setup(data, model) {
		super.setup(data, model);
	}

	dispose() {
		super.dispose();
	}

	render(slide0, slide1, progress) {
		super.render(slide0, slide1, progress);
	}

	resize(e) {
		super.resize(e);
	}
}